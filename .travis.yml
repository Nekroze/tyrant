language: python
python:
  - "2.7"
  - "3.3"
# command to install dependencies
install: 
  - "pip install pytest flake8 pylint --use-mirrors"
# command to run tests
script: 
  - py.test tyrant
  - flake8 tyrant
  - pylint --rcfile=.pylintrc tyrant
