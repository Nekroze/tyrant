language: python
python:
  - "2.7"
  - "3.3"
# command to install dependencies
install: 
  - "pip install six pytest flake8 --use-mirrors"
# command to run tests
script: 
  - py.test ./
  - flake8 tyrant
